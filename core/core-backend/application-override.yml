spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/dataease10?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: 100424
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    table: de_standalone_version
    validate-on-migrate: false
    locations: classpath:db/migration
    baseline-on-migrate: true
    out-of-order: true

mybatis-plus:
  mapper-locations: classpath:mybatis/*.xml

dataease:
  path:
    driver: ../../drivers
    ehcache: cache-local
