# "无效的表名"问题修复指南

## 🎯 问题确认

### 错误现象
保存数据集时提示：**无效的表名！**

### 错误原因
**Oracle表名大小写不匹配问题：**

1. Oracle数据库存储表名时默认使用**大写**
2. 代码在比较表名时**区分大小写**
3. 用户输入小写表名（如 `my_table`）与数据库中的大写表名（`MY_TABLE`）不匹配

---

## ✅ 已完成的代码修复

### 修复文件
`CalciteProvider.java` 第317-320行

### 修复内容
```java
// 修复前 ❌
if (!getTables(datasourceRequest).stream()
    .map(DatasetTableDTO::getTableName)
    .collect(Collectors.toList())
    .contains(table)) {
    DEException.throwException(Translator.get("i18n_invalid_table_name"));
}

// 修复后 ✅
List<String> availableTables = getTables(datasourceRequest).stream()
    .map(DatasetTableDTO::getTableName)
    .map(String::toUpperCase)  // 统一转大写
    .collect(Collectors.toList());

if (!availableTables.contains(table.toUpperCase())) {  // 输入也转大写
    DEException.throwException(Translator.get("i18n_invalid_table_name") +
        " [期望: " + table.toUpperCase() + ", 实际: " + table + "]");
}
```

---

## 🔧 应用修复的方法

### 方法1：使用IDEA重新编译（推荐）⭐

如果您使用IntelliJ IDEA：

1. **打开项目**
   ```
   File → Open → E:\cursor\dataease
   ```

2. **找到修改的文件**
   ```
   core/core-backend/src/main/java/io/dataease/datasource/provider/CalciteProvider.java
   ```

3. **重新编译项目**
   ```
   Build → Rebuild Project
   ```

4. **打包**
   ```
   Run → Edit Configurations →
   添加Maven配置 →
   Working directory: E:\cursor\dataease\core\core-backend
   Command line: clean package -DskipTests -Dmaven.antrun.skip=true
   ```

5. **运行**
   ```
   java -Dspring.profiles.active=desktop
        -Ddataease.path.driver=../../drivers
        -Ddataease.path.ehcache=./cache
        -jar target/CoreApplication.jar
   ```

### 方法2：临时解决方案（无需重新编译）

**在前端使用大写表名：**

1. **创建表时使用大写**
   ```sql
   CREATE TABLE EMPLOYEES (
       employee_id NUMBER,
       name VARCHAR2(100)
   );
   ```

2. **在DataEase中选择表时**
   - 表名列表会显示大写的表名
   - 直接选择即可（不要手动修改为小写）

### 方法3：创建Oracle同义词（变通方案）

如果需要使用小写表名，可以在Oracle中创建同义词：

```sql
-- 创建大写表
CREATE TABLE MY_TABLE (
    id NUMBER,
    name VARCHAR2(100)
);

-- 创建小写同义词
CREATE SYNONYM "my_table" FOR MY_TABLE;

-- 现在可以使用小写查询
SELECT * FROM "my_table";
```

---

## 📝 修复验证

修复后的效果对比：

| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| 表名 `EMPLOYEES`（大写） | ✅ 正常 | ✅ 正常 |
| 表名 `employees`（小写） | ❌ 无效的表名 | ✅ 正常 |
| 表名 `Employees`（混合） | ❌ 无效的表名 | ✅ 正常 |

---

## ⚠️ 当前编译问题

### 问题
```
JwtUtils.java: 找不到符号 withPayload
```

### 原因
这是项目中其他代码的依赖问题，与本次修复无关。

### 解决方案

**选项A：完整编译整个项目**
```bash
cd E:\cursor\dataease
mvn clean install -DskipTests -Dmaven.antrun.skip=true
```

**选项B：使用IDEA构建**
- IDEA的依赖管理更完善
- 可以自动处理依赖冲突

**选项C：使用已编译的jar（如果有）**
如果有之前成功编译的jar，可以反编译修改后重新打包

---

## 🚀 完整的编译命令

### 前提条件
- JDK 21
- Maven 3.6+
- 足够的内存（至少4GB）

### 编译步骤

```bash
# 1. 清理旧的编译文件
cd E:\cursor\dataease
mvn clean

# 2. 编译并安装到本地仓库
mvn install -DskipTests -Dmaven.antrun.skip=true

# 3. 编译后端模块
cd core/core-backend
mvn package -DskipTests -Dmaven.antrun.skip=true

# 4. 启动后端
cd target
java -Dspring.profiles.active=desktop ^
     -Ddataease.path.driver=../../drivers ^
     -Ddataease.path.ehcache=./cache ^
     -jar CoreApplication.jar
```

---

## 📞 如果需要帮助

### 检查点

1. **确认表名大小写**
   ```sql
   -- 在Oracle中查询实际的表名
   SELECT table_name FROM all_tables WHERE owner = 'YOUR_SCHEMA';
   ```

2. **检查数据源配置**
   - Schema名称是否正确
   - 用户是否有权限访问该Schema

3. **查看详细错误日志**
   ```bash
   tail -f E:\cursor\dataease\core\core-backend\logs\backend.log | grep -E "表名|table|TABLE"
   ```

### 收集诊断信息

如果问题持续，请提供：
1. Oracle表的实际名称（包括大小写）
2. 在DataEase中输入的表名
3. 后端日志中的错误堆栈
4. 数据源配置信息（Schema等）

---

## 📚 相关信息

- **Oracle表名规则**：未使用双引号的表名自动转为大写
- **DataEase表名处理**：需要在CalciteProvider中统一大小写
- **最佳实践**：在Oracle中统一使用大写表名
