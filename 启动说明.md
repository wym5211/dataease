# DataEase 服务启动指南

## 🌐 服务地址

### 当前运行状态

| 服务 | 地址 | 状态 |
|------|------|------|
| 🌐 **前端** | http://localhost:8082 | ✅ 运行中 |
| 🔧 **后端** | http://localhost:8100 | ✅ 运行中 |

**注意：** 前端端口可能因占用自动切换（8080/8081/8082）

---

## 🚀 快速启动

### 方式1：使用启动脚本（推荐）

**启动后端：**
```bash
双击运行：start-backend-dev.bat
```

**启动前端：**
```bash
双击运行：start-frontend-dev.bat
```

### 方式2：手动启动

**后端：**
```bash
cd E:\cursor\dataease\core\core-backend
java -Dspring.profiles.active=desktop ^
     -Ddataease.path.driver=../../drivers ^
     -Ddataease.path.ehcache=./cache ^
     -jar target/CoreApplication.jar
```

**前端：**
```bash
cd E:\cursor\dataease\core\core-frontend
npm run dev:win
```

---

## 📋 已修复的问题

### ✅ Oracle表名大小写兼容性
**问题：** 保存数据集时提示"无效的表名"

**修复内容：**
```java
// 现在支持任意大小写的表名
"EMPLOYEES"  ✅
"employees"  ✅
"Employees"  ✅
```

**修复文件：**
- `CalciteProvider.java:317-325`

### ✅ 自定义SQL支持
**问题：** 使用自定义SQL时出现NullPointerException

**修复内容：**
- 添加null检查
- 支持不指定表名的自定义SQL查询

### ✅ JWT API更新
**问题：** JWT工具类使用已废弃的API

**修复内容：**
- 更新为最新的JWT API
- 修复编译错误

### ✅ 前端依赖问题
**问题：** 缺少element-plus-secondary依赖

**修复内容：**
- 安装element-plus-secondary@1.0.0
- 清理Vite缓存
- 重新编译前端

---

## 💾 数据库信息

### H2数据库
- **JDBC URL**: `jdbc:h2:./data/desktop`
- **用户名**: `sa`
- **密码**: `123456`

### 默认登录
- **用户名**: `admin`
- **密码**: `DataEase@123456`

---

## 🔧 可用数据源

| 数据库 | 驱动 | 状态 |
|--------|------|------|
| Oracle | ojdbc10-19.19.0.0.jar | ✅ 已配置 |
| MySQL | mysql-connector-j-8.0.31.jar | ✅ 可用 |
| PostgreSQL | postgresql-42.3.6.jar | ✅ 可用 |
| SQL Server | mssql-jdbc-12.2.0.jre11.jar | ✅ 可用 |
| DB2 | db2jcc-db2jcc4.jar | ✅ 可用 |
| ClickHouse | clickhouse-jdbc-0.4.6.jar | ✅ 可用 |

---

## 📝 日志位置

### 后端日志
```
E:\cursor\dataease\core\core-backend\logs\
├── backend.log      # 主日志
├── error.log        # 错误日志
└── info.log         # 信息日志
```

### 查看日志
```bash
# 实时查看后端日志
tail -f E:\cursor\dataease\core\core-backend\logs\backend.log

# 查看错误
tail -f E:\cursor\dataease\core\core-backend\logs\error.log

# 过滤特定内容
grep -E "ORA-|表名|table" E:\cursor\dataease\core\core-backend\logs\backend.log
```

---

## ⚠️ 常见问题

### 端口被占用
```bash
# 查找占用端口的进程
netstat -ano | findstr ":8100"
netstat -ano | findstr ":8080"

# 停止进程
taskkill /F /PID <进程ID>
```

### Vite缓存错误
```bash
cd E:\cursor\dataease\core\core-frontend
rmdir /s /q node_modules\.vite
npm run dev:win
```

### 后端启动失败
```bash
# 清理缓存目录
cd E:\cursor\dataease\core\core-backend
rmdir /s /q cache

# 重新启动
```

---

## 📚 相关文档

- **开发环境使用说明**：`E:\cursor\dataease\开发环境使用说明.md`
- **表名大小写问题修复指南**：`E:\cursor\dataease\表名大小写问题修复指南.md`
- **SQL执行错误分析报告**：`E:\cursor\dataease\SQL执行错误分析报告.md`

---

## 🎯 验证步骤

### 1. 测试前端
访问：http://localhost:8082

### 2. 测试后端
访问：http://localhost:8100/h2-console

### 3. 测试Oracle数据集
1. 登录系统
2. 创建Oracle数据源
3. 创建数据集（选择任意大小写的表名）
4. 保存数据集 ✅
5. 预览数据 ✅

### 4. 测试自定义SQL
1. 在数据集中选择"SQL"模式
2. 输入SQL：`SELECT * FROM my_table WHERE ...`
3. 保存并预览 ✅

---

**所有功能已验证通过，可以正常使用！** 🎉
