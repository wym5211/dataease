# DataEase 开发环境启动指南

## 环境信息

✅ **Java**: JDK 21 (C:\Program Files\Eclipse Adoptium\jdk-21.0.6.7-hotspot)
✅ **Maven**: IntelliJ IDEA 内置 Maven
✅ **前端依赖**: 已安装 (1249个包)

## 配置文件

已创建开发环境配置文件：
- `core/core-backend/src/main/resources/application-dev.yml`
  - 使用H2内存数据库
  - 数据库路径：`./data/desktop`
  - 日志路径：`./logs`
  - 支持前端端口：8080和5173

## 启动方式

### 方式1：使用IntelliJ IDEA（推荐）⭐

1. **启动后端：**
   - 打开IDEA，定位到 `core/core-backend/src/main/java/io/dataease/CoreApplication.java`
   - 右键点击 `CoreApplication` 类
   - 选择 "Run 'CoreApplication'" 或 "Debug 'CoreApplication'"
   - 在运行配置中添加VM选项：`-Dspring.profiles.active=dev`
   - 或者编辑运行配置，添加环境变量：`SPRING_PROFILES_ACTIVE=dev`

2. **启动前端：**
   - 打开终端，进入 `core/core-frontend` 目录
   - 运行：`npm run dev:win`

### 方式2：使用命令行

**启动前端：**
```bash
cd core/core-frontend
npm run dev:win
```

前端将在 http://localhost:5173 启动

**启动后端（需要Maven跳过antrun）：**
```bash
cd core/core-backend

# 设置环境变量
set JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.6.7-hotspot
set PATH=%JAVA_HOME%\bin;%PATH%

# 启动后端（开发模式）
"C:\Program Files\JetBrains\IntelliJ IDEA 2023.2.1\plugins\maven\lib\maven3\bin\mvn.cmd" spring-boot:run -Dspring-boot.run.profiles=dev -Dmaven.antrun.skip=true
```

### 方式3：打包后运行

```bash
# 打包（跳过测试和前端复制）
cd core/core-backend
mvn clean package -DskipTests -Dmaven.antrun.skip=true

# 运行
java -Dspring.profiles.active=dev -jar target/core-backend.jar
```

## 注意事项

1. **H2数据库**：首次运行时会自动创建，数据存储在 `data/desktop` 目录
2. **Redis**：当前配置禁用了Redis健康检查，但应用仍可能尝试连接Redis
3. **日志**：后端日志会输出到 `logs` 目录
4. **端口**：
   - 后端：8100
   - 前端：5173（Vite开发服务器）

## 访问地址

- **前端界面**：http://localhost:5173
- **后端API**：http://localhost:8100
- **H2控制台**：http://localhost:8100/h2-console
  - JDBC URL: `jdbc:h2:./data/desktop`
  - 用户名: `sa`
  - 密码: `123456`

## 故障排查

**问题1：编译错误**
```bash
cd /e/cursor/dataease
mvn clean install -DskipTests -Dmaven.antrun.skip=true
```

**问题2：Redis连接错误**
- 开发环境下Redis连接错误不会阻止启动（已禁用健康检查）

**问题3：端口被占用**
- 修改 `application-dev.yml` 中的 `server.port` 配置
