# DataEase 开发环境使用说明

## ✅ 已完成的配置

### Oracle驱动问题解决方案

**问题原因：**
驱动路径配置错误，系统找不到 `drivers/` 目录中的Oracle驱动文件。

**解决方案：**
在启动命令中添加了正确的驱动路径参数：
```bash
-Ddataease.path.driver=../../drivers
```

---

## 🚀 快速启动

### 方式1：使用启动脚本（推荐）⭐

**启动后端：**
```bash
双击运行：start-backend-dev.bat
```

**启动前端：**
```bash
双击运行：start-frontend-dev.bat
```

### 方式2：手动启动

**后端启动命令：**
```bash
cd E:\cursor\dataease\core\core-backend

java -Dspring.profiles.active=desktop ^
     -Ddataease.path.driver=../../drivers ^
     -Ddataease.path.custom-drivers=../../custom-drivers/ ^
     -Ddataease.path.ehcache=./cache ^
     -jar target/CoreApplication.jar
```

**前端启动命令：**
```bash
cd E:\cursor\dataease\core\core-frontend
npm run dev:win
```

---

## 🌐 访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| **前端界面** | http://localhost:8081 | Vue.js 开发服务器 |
| **后端API** | http://localhost:8100 | Spring Boot 服务 |
| **H2控制台** | http://localhost:8100/h2-console | 数据库管理 |
| **API文档** | http://localhost:8100/doc.html | Knife4j 文档 |

---

## 💾 数据库信息

### H2数据库配置
- **JDBC URL**: `jdbc:h2:./data/desktop`
- **用户名**: `sa`
- **密码**: `123456`
- **数据库文件**: `E:\cursor\dataease\core\core-backend\data\desktop\`

### 默认登录信息
- **用户名**: `admin`
- **密码**: `DataEase@123456`

---

## 📁 目录结构

```
E:\cursor\dataease\
├── drivers/              # 数据库驱动目录
│   ├── ojdbc10-19.19.0.0.jar     # Oracle驱动 ✅
│   ├── mysql-connector-j-8.0.31.jar
│   ├── postgresql-42.3.6.jar
│   └── ...
├── core/
│   ├── core-frontend/   # 前端项目
│   │   └── node_modules/ # 前端依赖
│   └── core-backend/    # 后端项目
│       ├── target/
│       │   └── CoreApplication.jar  # 后端可执行jar
│       ├── cache/        # Ehcache缓存目录
│       ├── data/         # H2数据库文件
│       └── logs/         # 日志文件
├── start-backend-dev.bat # 后端启动脚本
└── start-frontend-dev.bat # 前端启动脚本
```

---

## 🔧 可用数据源驱动

项目已包含以下数据库驱动：

| 数据库 | 驱动文件 | 状态 |
|--------|----------|------|
| **Oracle** | ojdbc10-19.19.0.0.jar | ✅ 已配置 |
| **MySQL** | mysql-connector-j-8.0.31.jar | ✅ 可用 |
| **PostgreSQL** | postgresql-42.3.6.jar | ✅ 可用 |
| **SQL Server** | mssql-jdbc-12.2.0.jre11.jar | ✅ 可用 |
| **DB2** | db2jcc-db2jcc4.jar | ✅ 可用 |
| **ClickHouse** | clickhouse-jdbc-0.4.6.jar | ✅ 可用 |
| **Redshift** | redshift-jdbc42-2.1.0.1.jar | ✅ 可用 |
| **Impala** | ImpalaJDBC41.jar | ✅ 可用 |

---

## ⚠️ 常见问题

### 1. 端口被占用
```bash
# 查找占用8100端口的进程
netstat -ano | findstr ":8100"

# 停止该进程（将<PID>替换为实际的进程ID）
taskkill /F /PID <PID>
```

### 2. 缓存锁定错误
```bash
# 删除缓存目录
rm -rf E:\cursor\dataease\core\core-backend\cache
```

### 3. 前端依赖问题
```bash
cd E:\cursor\dataease\core\core-frontend
npm install
```

### 4. Vite缓存错误
```bash
cd E:\cursor\dataease\core\core-frontend
rmdir /s /q node_modules\.vite
npm run dev:win
```

---

## 📝 开发模式说明

当前使用的是 **Desktop 模式**：
- 使用H2内存数据库，无需MySQL
- 自动登录，无需输入账号密码
- 简化权限系统
- 适合前端开发和功能测试

如需完整功能（需要MySQL + Redis），请参考官方文档进行生产环境部署。

---

## 🔄 停止服务

**停止后端：**
在运行后端的命令行窗口按 `Ctrl+C`

**停止前端：**
在运行前端的命令行窗口按 `Ctrl+C`

或使用命令：
```bash
# 停止Java后端进程
taskkill /F /IM java.exe

# 停止Node前端进程
taskkill /F /IM node.exe
```

---

## 📚 更多信息

- **官方文档**: https://dataease.io/docs/
- **社区论坛**: https://bbs.fit2cloud.com/c/de/6
- **GitHub**: https://github.com/dataease/dataease
